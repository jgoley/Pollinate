<form>
	<li><input type="text" name="Name" value="{{group.Name}}"></li>
	<li><input type="text" name="hiveCount" value="{{group.hiveCount}}"></li>
	<input type="date" name="availableEnd" value={{group.availableEnd}} >
	<input type="date" name="availableBegin" value={{group.availableBegin}} >
	<div>
	<button class="update">Update</button>
	<button class="delete">Delete</button>
	</div>
</form>